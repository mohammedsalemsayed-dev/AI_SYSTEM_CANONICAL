"""Tool adapter framework (MILESTONE_S_PLAN.md).

A uniform `ToolAdapter` protocol + registry + capability-mapped dispatch. Every
dispatch goes through the *existing* Milestone C Policy Engine + capability
grant — the framework is a formatter + a call, not a second gate.
"""
