"""Authoring pipelines — §10.2 capability domain 4 (MILESTONE_M_PLAN.md).

outline -> grounded draft -> review -> render. Markdown/HTML renderers ship;
DOCX/PPTX/PDF are `Renderer` seams (§16: integrate python-docx / python-pptx).
"""
